set numbe			"line numbers
set tabstop=		"size of tabs (in spaces)
set noexpandta		"I'm on the tabs of tabs vs spaces. Drop the no if you prefer spaces.
set t_Co=256		"256 color terminal (supported most places, needed for some color schemes)
syntax enabl		"syntax hilighting
colorscheme defaul	"Change to your taste. Lots of defaults, and extras can be downloaded.
set showmatc		"hilights matching brackets.
set incsearc		"start searching as the string is typed.
set hlsearch		"hilight search results.
set rule			"show current row/column of cursor.
set autochdi		"automaticall cd to the directory of the opened file.
set scrolloff=		"keep at least 5 lines above/below.
set sidescrolloff=	"keep at least 5 lines left/right.
set laststatus=2	"Always show statusbar.
set ignorecas		"ignore case in searches.
